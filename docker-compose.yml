version: "3.9"

services:
  lunchleger:
    build: .
    container_name: lunchledger
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data   # persist SQLite across restarts
    restart: unless-stopped